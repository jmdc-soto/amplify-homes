type Customer @model @auth(rules: [{allow: public}]) {
  id: ID!
  num: Int
  name: String
  url: String
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
}
 